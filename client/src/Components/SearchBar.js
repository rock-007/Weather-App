const SearchBar = ({}) => {
    return(
        <div>
            <input type="text" placeholder="Search for City" />
            <button type='submit'>Search</button>
        </div>
        
    )

};

export default SearchBar;